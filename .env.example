# Copy to .env and set NODE_ENV=local | dev | prod
NODE_ENV=local

# Then create .env.local / .env.dev / .env.prod with full config.
# See .env.local in repo docs / team secret store for the full template.
